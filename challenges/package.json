{
  "name": "challenges",
  "version": "0.0.0",
  "scripts": {
    "dev": "wrangler dev --port 5002",
    "deploy": "fr-deploy-cli deploy",
    "test": "cargo test"
  },
  "dependencies": {
    "shared": "workspace:^"
  },
  "devDependencies": {
    "@devops/deploy-cli": "workspace:^"
  }
}
