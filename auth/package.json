{
  "name": "auth",
  "version": "0.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "fr-deployer-cli deploy",
    "test": "cargo test"
  },
  "devDependencies": {
    "@devops/deploy-cli": "workspace:^"
  }
}
