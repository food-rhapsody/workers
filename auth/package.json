{
  "name": "auth",
  "version": "0.0.0",
  "scripts": {
    "dev": "wrangler dev --port 5001",
    "deploy": "fr-deploy-cli deploy",
    "test": "cargo test"
  },
  "devDependencies": {
    "@devops/deploy-cli": "workspace:^"
  }
}
