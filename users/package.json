{
  "name": "users",
  "version": "0.0.0",
  "scripts": {
    "dev": "wrangler dev --port 5000",
    "deploy": "fr-deploy-cli deploy",
    "test": "cargo test"
  },
  "devDependencies": {
    "@devops/deploy-cli": "workspace:^"
  }
}
